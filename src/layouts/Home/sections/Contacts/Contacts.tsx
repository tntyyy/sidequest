import styles from './Contacts.module.scss';

const Contacts = () => {
    return (
        <div className={styles.wrapper}>
            <h2 className={styles.title}>
                Reach out to us: info@sidequest.com
            </h2>
        </div>
    );
};

export default Contacts;